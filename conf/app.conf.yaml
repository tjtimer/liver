STATIC_DIR: ./public/static
TEMPLATES_DIR: ./templates
DB_ADMIN: root:arango-pw
COMPONENTS:
  auth:
    database:
      name: auth-db
      user: auth:auth-pw
  stats:
    database:
      name: stats
      user: stats:stats-pw
  public:
    database:
      name: public
      user: public:public-pw
